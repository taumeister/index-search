[paths]
# Liste der Wurzelverzeichnisse, optional mit eigenem Label:
# /mnt/drive/projekte oder /mnt/drive/projekte:quelle_projekte
roots =/data/:unimatrix

[indexer]
worker_count = 2
# optionaler Cron-Ausdruck für geplante Läufe, z. B. 0 2 * * * für täglich 02:00
run_interval_cron =
# maximale Dateigröße in MB (optional, leer = kein Limit)
max_file_size_mb =

[smtp]
# Nur ausfüllen, wenn E-Mail-Report gewünscht
host = smtp.gmail.com
port = 587
use_tls = true
username = taumeister@gmail.com
password = avuuqgxbmagokcmu
from = index-sarch@unixuser.de
to = taumeister@gmail.com

[ui]
default_preview = panel
snippet_length = 240

[logging]
level = INFO
log_dir = logs
rotation_mb = 10
